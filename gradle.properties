org.gradle.jvmargs=-Xmx2048m
org.gradle.daemon=false
org.gradle.parallel=false

# Latest stable libGDX release as of this writing (change to bump version)
GDX_VERSION=1.14.2

# Android build-tools version used for d8 (must be installed by the workflow)
ANDROID_BUILD_TOOLS=34.0.0
ANDROID_COMPILE_SDK=34
